<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pathway_jogging">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pathway_jogging">
        <activity
            android:name=".app.login.login.LoginActivity"
            android:exported="true"
            android:label="@string/pathway_jogging" />
        <activity
            android:name=".app.landing.HomeActivity"
            android:exported="true"
            android:label="@string/title_home" />
        <activity
            android:name=".app.splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>